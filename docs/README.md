# docs/

Assets that support the repository's public surface — not user documentation
for the skill itself (that lives in `kali-master/SKILL.md` and its references).

| File | Purpose |
|---|---|
| `social-preview.html` | Editable source for the social preview card |
| `social-preview.png` | 1280×640 export — **set manually**: repo Settings → General → *Social preview* |

To iterate on the card: edit the HTML, render at exactly 1280×640, export PNG, re-upload in Settings.
