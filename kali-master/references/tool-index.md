# Kali 工具全量索引(470 工具)

> **用途**:查找具体工具时读本文件。每行:工具名 | 所属领域 | 用途摘要 | 安装命令。
> 详细用法进入对应领域文件(见文件名前缀)。

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 侦察/OSINT(recon)— 37 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `amass` | In-depth DNS Enumeration and Network Mapping | `sudo apt install amass` |
| `assetfinder` | Find domains and subdomains related to a given domain assetfinder is a command-line tool designed to find domains and subdomains associated with a specific doma | `sudo apt install assetfinder` |
| `caido` | Security auditing toolkit (desktop) | `sudo apt install caido` |
| `caido-cli` | Security auditing toolkit (CLI) | `sudo apt install caido-cli` |
| `dmitry` | Deepmagic Information Gathering Tool DMitry is a UNIX/(GNU)Linux command line application written in C. | `sudo apt install dmitry` |
| `dnsenum` | Tool to enumerate domain DNS information Dnsenum is a multithreaded perl script to enumerate DNS information of a domain and to discover non-contiguous ip block | `sudo apt install dnsenum` |
| `dnsmap` | DNS domain name brute forcing tool dnsmap scans a domain for common subdomains using a built-in or an external wordlist (if specified using -w option). | `sudo apt install dnsmap` |
| `dnsrecon` | Powerful DNS enumeration script DNSRecon is a Python script that provides the ability to perform: DNS Enumeration and Scanning Tool | `sudo apt install dnsrecon` |
| `dnstracer` | Trace DNS queries to the source dnstracer determines where a given Domain Name Server (DNS) gets its information from for a given hostname, and follows the chai | `sudo apt install dnstracer` |
| `dnswalk` | Checks dns zone information using nameserver lookups dnswalk is a DNS debugger. | `sudo apt install dnswalk` |
| `email2phonenumber` | OSINT tool to obtain a target’s phone number by having their email address | `sudo apt install email2phonenumber` |
| `emailharvester` | Email addresses harvester | `sudo apt install emailharvester` |
| `fierce` | Domain DNS scanner Fierce is a semi-lightweight scanner that helps locate non-contiguous IP space and hostnames against specified domains. | `sudo apt install fierce` |
| `finalrecon` | Fast and simple Python script for web reconnaissance A fast and simple Python script for web reconnaissance that follows a modular structure and provides detail | `sudo apt install finalrecon` |
| `findomain` | Fastest and most complete solution for domain recognition Findomain is fastest and most complete solution for domain recognition. | `sudo apt install findomain` |
| `gospider` | Fast web spider written in Go | `sudo apt install gospider` |
| `instaloader` | Instagram automatic photo downloader Instaloader downloads photos from Instagram, including public and private profiles, hashtags, user stories, feeds and saved | `sudo apt install instaloader` |
| `lbd` | Load balancer detector Checks if a given domain uses load-balancing. | `sudo apt install lbd` |
| `linkedin2username` | Generate username lists for companies on LinkedIn OSINT Tool: Generate username lists from companies on LinkedIn. | `sudo apt install linkedin2username` |
| `maltego` | Open source intelligence and forensics application Maltego is an open source intelligence and forensics application. | `sudo apt install maltego` |
| `massdns` | High-performance DNS stub resolver | `sudo apt install massdns` |
| `metagoofil` | Tool designed for extracting metadata of public documents Metagoofil is an information gathering tool designed for extracting metadata of public documents (pdf, | `sudo apt install metagoofil` |
| `owasp-mantra-ff` | Web application security testing framework built on top of Firefox Mantra is a browser especially designed for web application security testing. | `sudo apt install owasp-mantra-ff` |
| `parsero` | Robots.txt audit tool Parsero is a free script written in Python which reads the Robots.txt file of a web server and looks at the Disallow entries. | `sudo apt install parsero` |
| `photon` | Incredibly fast crawler designed for open source intelligence This package includes a fast and flexible crawler designed for open source intelligence (OSINT). | `sudo apt install photon` |
| `recon-ng` | Web Reconnaissance framework written in Python Recon-ng is a full-featured Web Reconnaissance framework written in Python. | `sudo apt install recon-ng` |
| `sherlock` | Find usernames across social networks Sherlock relies on the site’s designers providing a unique URL for a registered username. | `sudo apt install sherlock` |
| `spiderfoot` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `spiderfoot-cli` | OSINT collection and reconnaissance tool | `sudo apt install spiderfoot` |
| `subfinder` | Subdomain discovery tool | `sudo apt install subfinder` |
| `sublist3r` | Fast subdomains enumeration tool for penetration testers | `sudo apt install sublist3r` |
| `theHarvester` | Tool for gathering e-mail accounts and subdomain names from public sources The package contains a tool for gathering subdomain names, e-mail addresses, virtual  | `sudo apt install theharvester` |
| `tookie-osint` | OSINT information gathering tool for finding social media accounts Tookie-osint has a simple-to-use UI and is really straightforward. | `sudo apt install tookie-osint` |
| `uniscan-gui` | LFI, RFI, and RCE vulnerability scanner Uniscan is a simple Remote File Include, Local File Include and Remote Command Execution vulnerability scanner. | `sudo apt install uniscan` |
| `urlcrazy` | Domain typo generator Generate and test domain typos and variations to detect and perform typo squatting, URL hijacking, phishing, and corporate espionage. | `sudo apt install urlcrazy` |
| `uro` | Declutter URLs for crawling/pentesting Uro declutters a list of URLs by removing | `sudo apt install uro` |
| `wpprobe` | Fast WordPress plugin enumeration tool A fast WordPress plugin scanner that detects installed plugins via REST API enumeration and maps them to known vulnerabil | `sudo apt install wpprobe` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## 网络扫描(scanning)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apache-users` | Enumerate usernames on systems with Apache UserDir module This Perl script will enumerate the usernames on any system that uses Apache with the UserDir module. | `sudo apt install apache-users` |
| `atk6-thcping6` | The Hacker Choice’s IPv6 Attack Toolkit Attack toolkit for testing IPv6 and ICMPv6 protocol weaknesses. Some of the tools included: | `sudo apt install thc-ipv6` |
| `autorecon` | Multi-threaded network reconnaissance tool AutoRecon is a multi-threaded network reconnaissance tool which performs automated enumeration of services. | `sudo apt install autorecon` |
| `braa` | Mass SNMP scanner Braa is a mass snmp scanner. | `sudo apt install braa` |
| `enum4linux` | Enumerates info from Windows and Samba systems Enum4linux is a tool for enumerating information from Windows and Samba systems. | `sudo apt install enum4linux` |
| `enum4linux-ng` | Next generation version of enum4linux Next generation version of enum4linux (a Windows/Samba enumeration tool) with additional features like JSON/YAML export. | `sudo apt install enum4linux-ng` |
| `ike-scan` | Discover and fingerprint IKE hosts (IPsec VPN Servers) ike-scan discovers IKE hosts and can also fingerprint them using the retransmission backoff pattern. | `sudo apt install ike-scan` |
| `legion` | Semi-automated network penetration testing tool | `sudo apt install legion` |
| `masscan` | TCP port scanner MASSCAN is TCP port scanner which transmits SYN packets asynchronously and produces results similar to nmap, the most famous port scanner. | `sudo apt install masscan` |
| `mdb-sql` | Core library for accessing JET / MS Access (MDB) files Core library for accessing JET / MS Access database (MDB) files programmatically. | `sudo apt install libmdb3t64` |
| `mxcheck` | Info and security scanner for e-mail servers mxcheck is an info scanner for e-mail servers, checking: - DNS records: A, MX, PTR, SPF, MTA-STS, DKIM, DMARC - AS  | `sudo apt install mxcheck` |
| `nbtscan` | Scan networks searching for NetBIOS information NBTscan is a program for scanning IP networks for NetBIOS name information. | `sudo apt install nbtscan` |
| `nmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `onesixtyone` | Fast and simple SNMP scanner onesixtyone is a simple SNMP scanner which sends SNMP requests for the sysDescr value asynchronously with user-adjustable sending t | `sudo apt install onesixtyone` |
| `oscanner` | Oracle assessment framework Oscanner is an Oracle assessment framework developed in Java. | `sudo apt install oscanner` |
| `sctpscan` | SCTP network scanner for discovery and security SCTP network scanner for discovery and security | `sudo apt install sctpscan` |
| `sidguess` | Guesses sids against an Oracle database Guesses sids/instances against an Oracle database according to a predefined dictionary file. | `sudo apt install sidguesser` |
| `smtp-user-enum` | Username guessing tool for the SMTP service Username guessing tool primarily for use against the default Solaris SMTP service. | `sudo apt install smtp-user-enum` |
| `snmp-check` | SNMP service enumeration tool Like to snmpwalk, snmpcheck allows you to enumerate the SNMP devices and places the output in a very human readable friendly forma | `sudo apt install snmpcheck` |
| `sqlitebrowser` | GUI editor for SQLite databases SQLite Database Browser is a visual tool used to create, design and edit database files compatible with SQLite. | `sudo apt install sqlitebrowser` |
| `sslscan` | Tests SSL/TLS enabled services to discover supported cipher suites This tool allow queries SSL/TLS services (such as HTTPS) and reports the protocol versions, c | `sudo apt install sslscan` |
| `sslyze` | Fast and full-featured SSL scanner SSLyze is a Python tool that can analyze the SSL configuration of a server by connecting to it. | `sudo apt install sslyze` |
| `swaks` | SMTP command-line test tool swaks (Swiss Army Knife SMTP) is a command-line tool written in Perl for testing SMTP setups; it supports STARTTLS and SMTP AUTH (PL | `sudo apt install swaks` |
| `tlssled` | Evaluates the security of a target SSL/TLS (HTTPS) server TLSSLed is a Linux shell script whose purpose is to evaluate the security of a target SSL/TLS (HTTPS)  | `sudo apt install tlssled` |
| `tnscmd10g` | Tool to prod the oracle tnslsnr process A tool to prod the oracle tnslsnr process on port 1521/tcp. | `sudo apt install tnscmd10g` |
| `unicornscan` | Userland distributed TCP/IP stack Unicornscan is a new information gathering and correlation engine built for and by members of the security research and testin | `sudo apt install unicornscan` |
| `wafw00f` | Identify and fingerprint Web Application Firewall products This package identifies and fingerprints Web Application Firewall (WAF) products using the following  | `sudo apt install wafw00f` |
| `zenmap` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## Web 测试(web)— 33 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arjun` | HTTP parameter discovery suite This package can find query parameters for URL endpoints. | `sudo apt install arjun` |
| `beef-xss-start` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `beef-xss-stop` | Browser Exploitation Framework (BeEF) BeEF is short for The Browser Exploitation Framework. It is a penetration testing tool that focuses on the web browser. | `sudo apt install beef-xss` |
| `burpsuite` | Platform for security testing of web applications Burp Suite is an integrated platform for performing security testing of web applications. | `sudo apt install burpsuite` |
| `commix` | Automated All-in-One OS Command Injection and Exploitation Tool | `sudo apt install commix` |
| `crlfuzz` | Fast tool to scan CRLF vulnerability written in Go CRLFuzz is a tool to scan for CRLF vulnerabilities in a fast way using Go | `sudo apt install crlfuzz` |
| `davtest` | Testing tool for WebDAV servers DAVTest tests WebDAV enabled servers by uploading test executable files, and then (optionally) uploading files which allow for c | `sudo apt install davtest` |
| `dirb` | URL bruteforcing tool DIRB is a Web Content Scanner. It looks for existing (and/or hidden) Web Objects. | `sudo apt install dirb` |
| `dirbuster` | Web server directory brute-forcer DirBuster is a multi threaded java application designed to brute force directories and files names on web/application servers. | `sudo apt install dirbuster` |
| `dirsearch` | Web path scanner | `sudo apt install dirsearch` |
| `feroxbuster` | Fast, simple, recursive content discovery tool written in Rust feroxbuster is a tool designed to perform Forced Browsing. | `sudo apt install feroxbuster` |
| `ffuf` | Fast web fuzzer written in Go (program) ffuf is a fast web fuzzer written in Go that allows typical directory discovery, virtual host discovery (without DNS rec | `sudo apt install ffuf` |
| `gobuster` | High-performance discovery tool for directories, DNS and cloud storage Gobuster is a high-performance tool used to brute-force and discover: Gobuster is designe | `sudo apt install gobuster` |
| `joomscan` | OWASP Joomla Vulnerability Scanner Project | `sudo apt install joomscan` |
| `jsql` | Java tool for automatic database injection jSQL Injection is a lightweight application used to find database information from a distant server. | `sudo apt install jsql-injection` |
| `nikto` | Web server security scanner Nikto is a pluggable web server and CGI scanner written in Perl, using rfp’s LibWhisker to perform fast security or informational ch | `sudo apt install nikto` |
| `nuclei` | Fast and customizable vulnerability scanner based on simple YAML based DSL | `sudo apt install nuclei` |
| `paros` | Web application proxy Lightweight web application testing proxy | `sudo apt install paros` |
| `skipfish` | Fully automated, active web application security reconnaissance tool Skipfish is an active web application security reconnaissance tool. | `sudo apt install skipfish` |
| `sqlmap` | Automatic SQL injection tool sqlmap goal is to detect and take advantage of SQL injection vulnerabilities in web applications. | `sudo apt install sqlmap` |
| `sqlninja` | SQL server injection and takeover tool Fancy going from a SQL Injection on Microsoft SQL Server to a full GUI access on the DB? | `sudo apt install sqlninja` |
| `sqlsus` | MySQL injection tool sqlsus is an open source MySQL injection and takeover tool, written in perl. | `sudo apt install sqlsus` |
| `sstimap` | Automatic SSTI detection tool with interactive interface SSTImap is a penetration testing software that can check websites for Code Injection and Server-Side Te | `sudo apt install sstimap` |
| `subjack` | Subdomain Takeover tool | `sudo apt install subjack` |
| `tinja` | CLI tool for testing web pages for template injection TInjA is a CLI tool for testing web pages for template injection vulnerabilities and supports 44 of the mo | `sudo apt install tinja` |
| `wapiti` | Web application vulnerability scanner Wapiti allows you to audit the security of your web applications. It performs “black-box” scans, i.e. | `sudo apt install wapiti` |
| `watobo` | Semi-automated web application scanner WATOBO is intended to enable security professionals to perform highly efficient (semi-automated) web application security | `sudo apt install watobo` |
| `webscarab` | Web application review tool WebScarab is designed to be a tool for anyone who needs to expose the workings of an HTTP(S) based application, whether to allow the | `sudo apt install webscarab` |
| `wfuzz` | Web application bruteforcer Wfuzz is a tool designed for bruteforcing Web Applications, it can be used for finding resources not linked directories, servlets, s | `sudo apt install wfuzz` |
| `whatweb` | Next generation web scanner WhatWeb identifies websites. | `sudo apt install whatweb` |
| `wpscan` | Black box WordPress vulnerability scanner WPScan scans remote WordPress installations to find security issues. WordPress Security Scanner | `sudo apt install wpscan` |
| `xsser` | XSS testing framework Cross Site “Scripter” (aka XSSer) is an automatic -framework- to detect, exploit and report XSS vulnerabilities in web-based applications. | `sudo apt install xsser` |
| `zaproxy` | Testing tool for finding vulnerabilities in web applications The OWASP Zed Attack Proxy (ZAP) is an easy to use integrated penetration testing tool for finding  | `sudo apt install zaproxy` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 漏洞利用(exploit)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `armitage` | Cyber attack management for Metasploit Armitage is a scriptable red team collaboration tool for Metasploit that visualizes targets, recommends exploits, and exp | `sudo apt install armitage` |
| `backdoor-factory` | Injects shellcode into win32/64 PE files, 32/64bits ELF binaries, to continue normal file execution (if the shellcode supports it), by patching the exe/dll dire | `sudo apt install backdoor-factory` |
| `ccrypt` | Secure encryption and decryption of files and streams ccrypt is a utility for encrypting and decrypting files and streams. | `sudo apt install ccrypt` |
| `cymothoa` | Stealth backdooring tool Cymothoa is a stealth backdooring tool, that inject backdoor’s shellcode into an existing process. | `sudo apt install cymothoa` |
| `exe2hex` | Convert EXE to bat A Python script to convert a Windows PE executable file to a batch file and vice versa. | `sudo apt install exe2hexbat` |
| `exploitdb-papers` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `gophish-start` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `gophish-stop` | Open-Source Phishing Toolkit | `sudo apt install gophish` |
| `havoc` | Modern and malleable post-exploitation C2 framework Havoc is a modern, malleable post-exploitation command and control framework made for penetration testers, r | `sudo apt install havoc` |
| `heartleech` | Scanner detecting systems vulnerable to the heartbleed OpenSSL bug This is a typical “heartbleed” tool. | `sudo apt install heartleech` |
| `hoaxshell` | Windows reverse shell payload generator and handler that abuses http(s) Hoaxshell is a Windows reverse shell payload generator and handler that abuses the http( | `sudo apt install hoaxshell` |
| `koadic` | Windows post-exploitation rootkit | `sudo apt install koadic` |
| `laudanum` | Collection of injectable web files Laudanum is a collection of injectable files, designed to be used in a pentest when SQL injection flaws are found and are in  | `sudo apt install laudanum` |
| `metasploit-framework` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `msfpc` | MSFvenom Payload Creator (MSFPC) A quick way to generate various “basic” Meterpreter payloads using msfvenom which is part of the Metasploit framework. | `sudo apt install msfpc` |
| `msfvenom` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `padbuster` | Script for performing Padding Oracle attacks PadBuster is a Perl script for automating Padding Oracle Attacks. | `sudo apt install padbuster` |
| `phpggc` | Generate payloads that exploit unsafe object deserialization PHPGGC is a library of payloads exploiting unsafe object deserialization. | `sudo apt install phpggc` |
| `pompem` | Exploit and Vulnerability Finder Find exploit with a system of advanced search, designed to automate the search for Exploits and Vulnerability in the most impor | `sudo apt install pompem` |
| `searchsploit` | The Exploit Database’s archive of papers & ezines Searchable papers & ezines archives from The Exploit Database. https://www.exploit-db.com/papers | `sudo apt install exploitdb-papers` |
| `setoolkit` | Social-Engineer Toolkit The Social-Engineer Toolkit (SET) is an open-source Python-driven tool aimed at penetration testing around Social-Engineering. | `sudo apt install set` |
| `shellnoob` | Shellcode writing toolkit Features: * convert shellcode between different formats and sources. | `sudo apt install shellnoob` |
| `shellter` | Dynamic shellcode injection tool and dynamic PE infector Shellter is a dynamic shellcode injection tool aka dynamic PE infector. | `sudo apt install shellter` |
| `starkiller-start` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `starkiller-stop` | Frontend for Powershell Empire | `sudo apt install starkiller` |
| `veil` | Generates payloads to bypass anti-virus solutions Veil is a tool designed to generate metasploit payloads that bypass common anti-virus solutions. | `sudo apt install veil` |
| `villain` | High level C2 framework Villain is a C2 framework that can handle multiple TCP socket & HoaxShell-based reverse shells, enhance their functionality with additio | `sudo apt install villain` |
| `webacoo` | Web backdoor cookie script kit Scripts for creating Web backdoors using cookies, with module support | `sudo apt install webacoo` |
| `webshells` | Collection of webshells A collection of webshells for ASP, ASPX, CFM, JSP, Perl, and PHP servers. | `sudo apt install webshells` |
| `weevely` | Stealth tiny web shell Weevely is a stealth PHP web shell that simulate telnet-like connection. | `sudo apt install weevely` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 密码攻击(passwords)— 43 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `bopscrk` | Generate smart and powerful wordlists Targeted-attack wordlist creator: introduce personal info related to target, combines every word and transforms results in | `sudo apt install bopscrk` |
| `cewl` | Custom word list generator CeWL (Custom Word List generator) is a ruby app which spiders a given URL, up to a specified depth, and returns a list of words which | `sudo apt install cewl` |
| `chntpw` | NT SAM password recovery utility This little program provides a way to view information and change user passwords in a Windows NT/2000 user database file. | `sudo apt install chntpw` |
| `cmospwd` | Decrypt BIOS passwords from CMOS CmosPwd is a cross-platform tool to decrypt password stored in CMOS used to access a computer’s BIOS setup. | `sudo apt install cmospwd` |
| `creddump7` | Python tool to extract credentials and secrets from Windows registry hives | `sudo apt install creddump7` |
| `crowbar` | Brute forcing tool | `sudo apt install crowbar` |
| `crunch` | Tool for creating wordlist Crunch is a wordlist generator where you can specify a standard character set or any set of characters to be used in generating the w | `sudo apt install crunch` |
| `eapmd5pass` | Tool for extracting and cracking EAP-MD5 EAP-MD5 is a legacy authentication mechanism that does not provide sufficient protection for user authentication creden | `sudo apt install eapmd5pass` |
| `fcrackzip` | Password cracker for zip archives fcrackzip is a fast password cracker partly written in assembler. | `sudo apt install fcrackzip` |
| `freeradius` | High-performance and highly configurable RADIUS server FreeRADIUS is a high-performance RADIUS server with support for: See if a user is (still) logged in on a  | `sudo apt install freeradius` |
| `gitxray` | Scan GitHub repositories and contributors to collect data Gitxray (short for Git X-Ray) is a multifaceted security tool designed for use on GitHub repositories. | `sudo apt install gitxray` |
| `hash-identifier` | Tool to identify hash types Software to identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hash-identifier` |
| `hashcat` | World’s fastest and most advanced password recovery utility Hashcat supports five unique modes of attack for over 300 highly-optimized hashing algorithms. | `sudo apt install hashcat` |
| `hashid` | Identify the different types of hashes used to encrypt data Identify the different types of hashes used to encrypt data and especially passwords. | `sudo apt install hashid` |
| `hydra` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `hydra-gtk` | Very fast network logon cracker Hydra is a parallelized login cracker which supports numerous protocols to attack. | `sudo apt install hydra` |
| `john` | Active password cracking tool John the Ripper is a tool designed to help systems administrators to find weak (easy to guess or crack through brute force) passwo | `sudo apt install john` |
| `johnny` | GUI for John the Ripper Johnny is provides a GUI for the John the Ripper password cracking tool. | `sudo apt install johnny` |
| `legba` | Multiprotocol credentials bruteforcer / password sprayer and enumerator Legba is a multiprotocol credentials bruteforcer / password sprayer and enumerator built | `sudo apt install legba` |
| `maskgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `medusa` | Fast, parallel, modular, login brute-forcer for network services Medusa is intended to be a speedy, massively parallel, modular, login brute-forcer. | `sudo apt install medusa` |
| `mifare-classic-format` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mimikatz` | Uses admin rights on Windows to display passwords in plaintext Mimikatz uses admin rights on Windows to display passwords of currently logged in users in plaint | `sudo apt install mimikatz` |
| `ncrack` | High-speed network authentication cracking tool Ncrack is a high-speed network authentication cracking tool. | `sudo apt install ncrack` |
| `ophcrack` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `ophcrack-cli` | Microsoft Windows password cracker using rainbow tables (gui) Ophcrack is a Windows password cracker based on a time-memory trade-off using rainbow tables. | `sudo apt install ophcrack` |
| `patator` | Multi-purpose brute-forcer Patator is a multi-purpose brute-forcer, with a modular design and a flexible usage. Currently it supports the following modules: | `sudo apt install patator` |
| `policygen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `rcrack` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rcracki_mt` | Rainbow table password cracker RainbowCrack is a general propose implementation of Philippe Oechslin’s faster time-memory trade-off technique. | `sudo apt install rainbowcrack` |
| `rsmangler` | Wordlist mangling tool RSMangler will take a wordlist and perform various manipulations on it similar to those done by John the Ripper the main difference being | `sudo apt install rsmangler` |
| `samdump2` | Dump Windows 2k/NT/XP password hashes This tool is designed to dump Windows 2k/NT/XP password hashes from a SAM file, using the syskey bootkey from the system h | `sudo apt install samdump2` |
| `seclists` | Collection of multiple types of security lists SecLists is a collection of multiple types of lists used during security assessments. | `sudo apt install seclists` |
| `sipcrack` | SIP login dumper/cracker The tools contained in this package offer support for pcap files, wordlists and many more to extract all needed information and brutefo | `sudo apt install sipcrack` |
| `sqldict` | Dictionary attack tool for SQL Server SQLdict is a dictionary attack tool for SQL Server. | `sudo apt install sqldict` |
| `statsgen` | Password analysis and cracking kit PACK was developed in order to aid in a password cracking competition “Crack Me If You Can” that occurred during Defcon 2010. | `sudo apt install pack` |
| `sucrack` | Multithreaded su bruteforcer sucrack is a multithreaded Linux/UNIX tool for cracking local user accounts via wordlist bruteforcing su. | `sudo apt install sucrack` |
| `thc-pptp-bruter` | THC PPTP Brute Force Brute force program against pptp vpn endpoints (tcp port 1723). Fully standalone. Supports latest MSChapV2 authentication. | `sudo apt install thc-pptp-bruter` |
| `truecrack` | Bruteforce password cracker for TrueCrypt volumes TrueCrack is a bruteforce password cracker for TrueCrypt (Copyright) volume. | `sudo apt install truecrack` |
| `trufflehog` | Searches through git repositories for secrets | `sudo apt install trufflehog` |
| `twofi` | Twitter words of interest When attempting to crack passwords custom word lists are very useful additions to standard dictionaries. | `sudo apt install twofi` |
| `wordlists` | Contains the rockyou wordlist | `sudo apt install wordlists` |
| `xspy` | X server sniffer Sniffs keystrokes on remote or local X-Windows servers. | `sudo apt install xspy` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 无线安全(wireless)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `aircrack-ng` | Wireless WEP/WPA cracking utilities aircrack-ng is an 802.11a/b/g WEP/WPA cracking program that can recover a 40-bit, 104-bit, 256-bit or 512-bit WEP key once e | `sudo apt install aircrack-ng` |
| `airgeddon` | Multi-use bash script for Linux systems to audit wireless networks airgeddon is a menu driven 3rd party tools wrapper to audit wireless networks with many featu | `sudo apt install airgeddon` |
| `asleap` | A tool for exploiting Cisco LEAP networks Demonstrates a serious deficiency in proprietary Cisco LEAP networks. | `sudo apt install asleap` |
| `bluelog` | Bluetooth scanner and logger Bluelog is a Bluetooth scanner designed to tell you how many discoverable devices there are in an area as quickly as possible. | `sudo apt install bluelog` |
| `blueranger` | Simple Bash script to locate Bluetooth devices BlueRanger is a simple Bash script which uses Link Quality to locate Bluetooth device radios. | `sudo apt install blueranger` |
| `bluesnarfer` | Bluesnarfing utility A bluetooth bluesnarfing utility | `sudo apt install bluesnarfer` |
| `btscanner` | Ncurses-based scanner for Bluetooth devices btscanner is a tool designed specifically to extract as much information as possible from a Bluetooth device without | `sudo apt install btscanner` |
| `bully` | Implementation of the WPS brute force attack, written in C Bully is a new implementation of the WPS brute force attack, written in C. | `sudo apt install bully` |
| `chirp` | Configuration tool for amateur radios CHIRP is a free, open-source tool for programming your amateur radio. | `sudo apt install chirp` |
| `cowpatty` | Brute-force WPA dictionary attack If you are auditing WPA-PSK or WPA2-PSK networks, you can use this tool to identify weak passphrases that were used to generat | `sudo apt install cowpatty` |
| `crackle` | Crack and decrypt BLE encryption crackle exploits a flaw in the BLE pairing process that allows an attacker to guess or very quickly brute force the TK (Tempora | `sudo apt install crackle` |
| `fern-wifi-cracker` | Automated Wi-Fi cracker | `sudo apt install fern-wifi-cracker` |
| `gnuradio` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `gqrx` | GNU Radio Software Radio Toolkit GNU Radio provides signal processing blocks to implement software radios. | `sudo apt install gnuradio` |
| `hackrf_info` | Software defined radio peripheral - utilities HackRF is an open source Software Defined Radio that can receive and transmit between 30 MHz and 6 GHz. | `sudo apt install hackrf` |
| `kismet` | Wireless network and device detector (metapackage) Kismet is a wireless network and device detector, sniffer, wardriving tool, and WIDS (wireless intrusion dete | `sudo apt install kismet` |
| `mfcuk` | MiFare Classic Universal toolKit Toolkit containing samples and various tools based on and around libnfc and crapto1, with emphasis on Mifare Classic NXP/Philip | `sudo apt install mfcuk` |
| `mfoc` | MIFARE Classic offline cracker MFOC is an open source implementation of “offline nested” attack by Nethemba. | `sudo apt install mfoc` |
| `mfterm` | Terminal for working with Mifare Classic 1-4k Tags mfterm is a terminal interface for working with Mifare Classic tags. | `sudo apt install mfterm` |
| `pixiewps` | Offline WPS bruteforce tool Pixiewps is a tool written in C used to bruteforce offline the WPS pin exploiting the low or non-existing entropy of some APs (pixie | `sudo apt install pixiewps` |
| `reaver` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `rfcat` | Swiss army knife of sub-GHz radio Rfcat is a sub GHz analysis tool. | `sudo apt install rfcat` |
| `sparrow-wifi` | Graphical Wi-Fi Analyzer for Linux | `sudo apt install sparrow-wifi` |
| `spooftooph` | Automates spoofing or cloning Bluetooth devices Spooftooph is designed to automate spoofing or cloning Bluetooth device Name, Class, and Address. | `sudo apt install spooftooph` |
| `ubertooth-util` | Shared library for Bluetooth experimentation - development files Project Ubertooth is an open source wireless development platform suitable for Bluetooth experi | `sudo apt install libubertooth-dev` |
| `wash` | Brute force attack tool against Wi-Fi Protected Setup PIN number Reaver performs a brute force attack against an access point’s Wi-Fi Protected Setup pin number | `sudo apt install reaver` |
| `wifi-honey` | Wi-Fi honeypot In the case of WPA/WPA2, by running airodump-ng along side this you also end up capturing the first two packets of the four way handshake and so  | `sudo apt install wifi-honey` |
| `wifiphisher` | Automated phishing attacks against Wi-Fi networks | `sudo apt install wifiphisher` |
| `wifipumpkin3` | Powerful framework for rogue access point attack | `sudo apt install wifipumpkin3` |
| `wifite` | Python script to automate wireless auditing using aircrack-ng tools Wifite is a tool to audit WEP or WPA encrypted wireless networks. | `sudo apt install wifite` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## 网络攻防(network)— 75 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `0trace.sh` | Traceroute tool that can run within an existing TCP connection The package is traceroute tool that can be run within an existing, open TCP connection, therefore | `sudo apt install 0trace` |
| `above` | Network security sniffer for finding vulnerabilities in the network | `sudo apt install above` |
| `arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `arpspoof` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `arpwatch` | Ethernet/FDDI station activity monitor Arpwatch maintains a database of Ethernet MAC addresses seen on the network, with their associated IP pairs. | `sudo apt install arpwatch` |
| `bettercap` | Complete, modular, portable and easily extensible MITM framework The Swiss Army knife for 802.11, BLE, IPv4 and IPv6 networks reconnaissance and MITM attacks. | `sudo apt install bettercap` |
| `cge.pl` | Simple and fast Cisco exploitation tool Cisco Global Exploiter (CGE), is an advanced, simple and fast security testing tool. | `sudo apt install cisco-global-exploiter` |
| `cisco-ocs` | Mass Cisco scanner A mass Cisco scanning tool. | `sudo apt install cisco-ocs` |
| `cisco-torch` | Cisco device scanner The main feature that makes cisco-torch different from similar tools is the extensive use of forking to launch multiple scanning processes  | `sudo apt install cisco-torch` |
| `copy-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `darkstat` | Network traffic analyzer It is a packet sniffer which runs as a background process and serves its statistics to a web browser: It is known to be smaller (in ter | `sudo apt install darkstat` |
| `dhcpig` | DHCP exhaustion script using scapy network library DHCPig initiates an advanced DHCP exhaustion attack. | `sudo apt install dhcpig` |
| `dnschef` | DNS proxy for penetration testers DNSChef is a highly configurable DNS proxy for Penetration Testers and Malware Analysts. | `sudo apt install dnschef` |
| `driftnet` | Picks out and displays images from network traffic Inspired by EtherPEG, Driftnet is a program which listens to network traffic and picks out images from TCP st | `sudo apt install driftnet` |
| `dsniff` | Various tools to sniff network traffic for cleartext insecurities | `sudo apt install dsniff` |
| `enumiax` | IAX protocol username enumerator enumIAX is an Inter Asterisk Exchange protocol username brute-force enumerator. | `sudo apt install enumiax` |
| `ettercap` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `ettercap-text-only` | Multipurpose sniffer/interceptor/logger for switched LAN Ettercap supports active and passive dissection of many protocols (even encrypted ones) and includes ma | `sudo apt install ettercap-common` |
| `evilginx2` | Man-in-the-middle attack framework | `sudo apt install evilginx2` |
| `ferret-sidejack` | Monitors data and extracts interesting data This tool extracts interesting bits from network traffic. One use is to feed the “hamster” tool. | `sudo apt install ferret-sidejack` |
| `fiked` | Cisco VPN attack tool FakeIKEd, or fiked for short, is a fake IKE daemon supporting just enough of the standards and Cisco extensions to attack commonly found i | `sudo apt install fiked` |
| `firewalk` | Active reconnaissance network security tool Firewalk is an active reconnaissance network security tool that attempts to determine what layer 4 protocols a given | `sudo apt install firewalk` |
| `fluxion` | Security auditing and social-engineering research tool Fluxion is a security auditing and social-engineering research tool. | `sudo apt install fluxion` |
| `fping` | Sends ICMP ECHO_REQUEST packets to network hosts fping is a ping like program which uses the Internet Control Message Protocol (ICMP) echo request to determine  | `sudo apt install fping` |
| `fragrouter` | IDS evasion toolkit Fragrouter is a network intrusion detection evasion toolkit. Network intrusion detection evasion toolkit | `sudo apt install fragrouter` |
| `goldeneye` | HTTP DoS test tool GoldenEye is a HTTP DoS Test Tool. This tool can be used to test if a site is susceptible to Deny of Service (DoS) attacks. | `sudo apt install goldeneye` |
| `hamster-sidejack` | Sidejacking tool Hamster is tool or “sidejacking”. | `sudo apt install hamster-sidejack` |
| `hexinject` | Versatile packet injector and sniffer HexInject is a very versatile packet injector and sniffer, that provide a command-line framework for raw network access. | `sudo apt install hexinject` |
| `hping3` | Active Network Smashing Tool hping3 is a network tool able to send custom ICMP/UDP/TCP packets and to display target replies like ping does with ICMP replies. | `sudo apt install hping3` |
| `httrack` | Copy websites to your computer (Offline browser) HTTrack is an offline browser utility, allowing you to download a World Wide website from the Internet to a loc | `sudo apt install httrack` |
| `iaxflood` | VoIP flooder tool A UDP Inter-Asterisk_eXchange (i.e. IAX) packet was captured from an IAX channel between two Asterisk IP PBX’s. | `sudo apt install iaxflood` |
| `intrace` | Traceroute-like application piggybacking on existing TCP connections InTrace is a traceroute-like application that enables users to enumerate IP hops exploiting | `sudo apt install intrace` |
| `inviteflood` | SIP/SDP INVITE message flooding over UDP/IP A tool to perform SIP/SDP INVITE message flooding over UDP/IP. | `sudo apt install inviteflood` |
| `iputils-arping` | Sends IP and/or ARP pings (to the MAC address) The arping utility sends ARP and/or ICMP requests to the specified host and displays the replies. | `sudo apt install arping` |
| `macchanger` | Utility for manipulating the MAC address of network interfaces GNU MAC Changer is an utility that makes the maniputation of MAC addresses of network interfaces  | `sudo apt install macchanger` |
| `mdk3` | Wireless attack tool for IEEE 802.11 networks MDK is a proof-of-concept tool to exploit common IEEE 802.11 (Wi-Fi) protocol weaknesses. | `sudo apt install mdk3` |
| `merge-router-config.pl` | Copies Cisco configs via SNMP This package copies configuration files from Cisco devices running SNMP. | `sudo apt install copy-router-config` |
| `mitm6` | Pwning IPv4 via IPv6 mitm6 is a pentesting tool that exploits the default configuration of Windows to take over the default DNS server. | `sudo apt install mitm6` |
| `mitmproxy` | SSL-capable man-in-the-middle HTTP proxy mitmproxy is an interactive man-in-the-middle proxy for HTTP and HTTPS. | `sudo apt install mitmproxy` |
| `netdiscover` | Active/passive network address scanner using ARP requests Netdiscover is an active/passive address reconnaissance tool, mainly developed for those wireless netw | `sudo apt install netdiscover` |
| `netmask` | Helps determine network masks This is a tiny program handy if you work with firewalls or routers occasionally (possibly using this as a helper for shell scripts | `sudo apt install netmask` |
| `netsniff-ng` | Linux network packet sniffer toolkit netsniff-ng is a high performance Linux network sniffer for packet inspection. | `sudo apt install netsniff-ng` |
| `ohrwurm` | RTP fuzzer ohrwurm is a small and simple RTP fuzzer that has been successfully tested on a small number of SIP phones. | `sudo apt install ohrwurm` |
| `p0f` | Passive OS fingerprinting tool p0f performs passive OS detection based on SYN packets. Unlike nmap and queso, p0f does recognition without sending any data. | `sudo apt install p0f` |
| `protos-sip` | SIP test suite The purpose of this test-suite is to evaluate implementation level security and robustness of Session Initiation Protocol (SIP) implementations. | `sudo apt install protos-sip` |
| `rtpbreak` | Detects, reconstructs, and analyzes RTP sessions With rtpbreak you can detect, reconstruct and analyze any RTP session. | `sudo apt install rtpbreak` |
| `rtpflood` | Tool to flood any RTP device A command line tool used to flood any device that is processing RTP. | `sudo apt install rtpflood` |
| `rtpinsertsound` | Inserts audio into a specified stream A tool to insert audio into a specified audio (i.e. RTP) stream was created in the August - September 2006 timeframe. | `sudo apt install rtpinsertsound` |
| `rtpmixsound` | Mixes pre-recorded audio in real-time A tool to mix pre-recorded audio in real-time with the audio (i.e. RTP) in the specified target audio stream. | `sudo apt install rtpmixsound` |
| `sara` | RouterOS Security Inspector | `sudo apt install sara` |
| `scapy` | Packet generator/sniffer and network scanner/discovery (Python 3) Scapy is a powerful interactive packet manipulation tool, packet generator, network scanner, n | `sudo apt install python3-scapy` |
| `siege` | HTTP regression testing and benchmarking utility Siege is an regression test and benchmark utility. | `sudo apt install siege` |
| `siparmyknife` | SIP fuzzing tool SIP Army Knife is a fuzzer that searches for cross site scripting, SQL injection, log injection, format strings, buffer overflows, and more. | `sudo apt install siparmyknife` |
| `sipp` | Traffic generator for the SIP protocol SIPp is a free Open Source test tool / traffic generator for the SIP protocol. | `sudo apt install sipp` |
| `sippts` | Set of tools to audit SIP based VoIP Systems Sippts is a set of tools to audit VoIP servers and devices using SIP protocol. | `sudo apt install sippts` |
| `sipsak` | SIP Swiss army knife sipsak is a small command line tool for developers and administrators of Session Initiation Protocol (SIP) applications. | `sudo apt install sipsak` |
| `slowhttptest` | Application layer Denial of Service attacks simulation tool SlowHTTPTest is a highly configurable tool that simulates some application layer Denial of Service a | `sudo apt install slowhttptest` |
| `sniffjoke` | Transparent TCP connection scrambler SniffJoke is an application for Linux that handle transparently your TCP connection, delaying, modifyng and inject fake pac | `sudo apt install sniffjoke` |
| `ssldump` | SSLv3/TLS network protocol analyzer This program will dump the traffic on a network and analyze it for SSLv3/TLS network traffic, typically used to secure TCP c | `sudo apt install ssldump` |
| `sslsniff` | SSL/TLS man-in-the-middle attack tool sslsniff is designed to create man-in-the-middle (MITM) attacks for SSL/TLS connections, and dynamically generates certs f | `sudo apt install sslsniff` |
| `sslsplit` | Transparent and scalable SSL/TLS interception SSLsplit is a tool for man-in-the-middle attacks against SSL/TLS encrypted network connections. | `sudo apt install sslsplit` |
| `svcrack` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svcrash` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svmap` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svreport` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `svwar` | Tools to audit SIP based VoIP systems SIPVicious suite is a set of tools that can be used to audit SIP based VoIP systems. | `sudo apt install sipvicious` |
| `t50` | Multi-protocol packet injector tool Multi-protocol packet injector tool for *nix systems, actually supporting 15 protocols. Features: | `sudo apt install t50` |
| `tcpdump` | Command-line network traffic analyzer This program allows you to dump the traffic on a network. | `sudo apt install tcpdump` |
| `tcpflow` | TCP flow recorder tcpflow is a program that captures data transmitted as part of TCP connections (flows), and stores the data in a way that is convenient for pr | `sudo apt install tcpflow` |
| `tcpreplay` | Tool to replay saved tcpdump files at arbitrary speeds Tcpreplay is aimed at testing the performance of a NIDS by replaying real background network traffic in w | `sudo apt install tcpreplay` |
| `termineter` | Smart meter testing framework | `sudo apt install termineter` |
| `thc-ssl-dos` | Stress tester for the SSL handshake THC-SSL-DOS is a tool to verify the performance of SSL. | `sudo apt install thc-ssl-dos` |
| `voiphopper` | Runs a VLAN hop security test VoIP Hopper is a GPLv3 licensed security tool, written in C that rapidly runs a VLAN Hop security test. | `sudo apt install voiphopper` |
| `wireshark` | Network packet dissection library – data files The libwireshark library provides the network packet dissection services developed by the Wireshark project. | `sudo apt install libwireshark-data` |
| `yersinia` | Network vulnerabilities check software Yersinia is a framework for performing layer 2 attacks. | `sudo apt install yersinia` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## AD/Windows 内网(adwin)— 28 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `azurehound` | BloodHound data collector for Microsoft Azure (program) | `sudo apt install azurehound` |
| `bloodhound` | Six Degrees of Domain Admin, BloodHound CE | `sudo apt install bloodhound` |
| `bloodhound-ce-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodhound-python` | Python based ingestor for BloodHound CE | `sudo apt install bloodhound-ce-python` |
| `bloodyad` | Active Directory privilege escalation framework bloodyAD can perform specific LDAP calls to a domain controller in order to perform AD privesc. | `sudo apt install bloodyad` |
| `crackmapexec` | Swiss army knife for pentesting networks This package is a swiss army knife for pentesting Windows/Active Directory environments. | `sudo apt install crackmapexec` |
| `evil-winrm` | Ultimate WinRM shell for hacking/pentesting | `sudo apt install evil-winrm` |
| `evil-winrm-py` | Execute commands on remote Windows machines using WinRM Python-based tool for executing commands on remote Windows machines using the WinRM (Windows Remote Mana | `sudo apt install evil-winrm-py` |
| `impacket-mssqlclient` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-psexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-scripts` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbexec` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `impacket-smbserver` | Links to useful impacket scripts examples | `sudo apt install impacket-scripts` |
| `kerberoast` | Tools for attacking MS Kerberos implementations | `sudo apt install kerberoast` |
| `krbrelayx` | Kerberos relaying and unconstrained delegation abuse toolkit Kerberos relaying and unconstrained delegation abuse toolkit. | `sudo apt install krbrelayx` |
| `ldeep` | In-depth ldap enumeration utility ldeep is an in-depth ldap enumeration utility that can either run against an Active Directory LDAP server or locally on saved  | `sudo apt install ldeep` |
| `netexec` | Network Execution Tool NetExec (AKA nxc) is a network service exploitation tool that helps automate assessing the security of large networks. | `sudo apt install netexec` |
| `nishang` | Collection of PowerShell scripts and payloads Nishang is a framework and collection of scripts and payloads which enables usage of PowerShell for offensive secu | `sudo apt install nishang` |
| `passing-the-hash` | Patched tools to use password hashes as authentication input | `sudo apt install passing-the-hash` |
| `powershell-empire` | PowerShell and Python post-exploitation agent | `sudo apt install powershell-empire` |
| `powersploit` | PowerShell Post-Exploitation Framework PowerSploit is a series of Microsoft PowerShell scripts that can be used in post-exploitation scenarios during authorized | `sudo apt install powersploit` |
| `pwsh` | PowerShell is an automation and configuration management platform. It consists of a cross-platform command-line shell and associated scripting language. | `sudo apt install powershell` |
| `rdesktop` | RDP client for Windows NT/2000 Terminal Server and Windows Servers rdesktop is an open source client for Windows NT/2000 Terminal Server and Windows Server 2003 | `sudo apt install rdesktop` |
| `responder` | LLMNR/NBT-NS/mDNS Poisoner | `sudo apt install responder` |
| `rubeus` | Raw Kerberos interaction and abuses Rubeus is a C# toolset for raw Kerberos interaction and abuses. | `sudo apt install rubeus` |
| `sharphound` | C# Data Collector for BloodHound | `sudo apt install sharphound` |
| `smbmap` | Handy SMB enumeration tool SMBMap allows users to enumerate samba share drives across an entire domain. | `sudo apt install smbmap` |
| `xfreerdp3` | FreeRDP proxy server FreeRDP is a libre client/server implementation of the Remote Desktop Protocol (RDP). | `sudo apt install freerdp-proxy` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 隧道/C2(tunnels)— 30 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `cadaver` | Command-line WebDAV client cadaver supports file upload, download, on-screen display, in-place editing, namespace operations (move/copy), collection creation an | `sudo apt install cadaver` |
| `chisel` | Fast TCP/UDP tunnel over HTTP (program) | `sudo apt install chisel` |
| `chisel-common-binaries` | Prebuilt binaries for chisel | `sudo apt install chisel-common-binaries` |
| `dbd` | Netcat clone with encryption dbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install dbd` |
| `dns2tcpc` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dns2tcpd` | TCP-over-DNS tunnel server and client dns2tcp is a set of tools to encapsulate a TCP session in DNS packets. | `sudo apt install dns2tcp` |
| `dnscat` | DNS tunnel (metapackage) This tool is designed to create an encrypted command-and-control (C&C) channel over the DNS protocol, which is an effective tunnel out  | `sudo apt install dnscat2` |
| `goshs` | SimpleHTTPServer written in Go This tool provides a SimpleHTTPServer written in Go, enhanced with features and security. | `sudo apt install goshs` |
| `iodine-client-start` | Tool for tunneling IPv4 data through a DNS server This is a piece of software that lets you tunnel IPv4 data through a DNS server. | `sudo apt install iodine` |
| `ligolo-agent` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-mp` | Multiplayer pivoting solution Ligolo-MP is an advanced version of Ligolo-ng, with client-server architecture, enabling pentesters to play with multiple concurre | `sudo apt install ligolo-mp` |
| `ligolo-mp-client` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `ligolo-ng-common-binaries` | Prebuilt binaries for Advanced ligolo-ng Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tunnels from a reverse TCP/TLS con | `sudo apt install ligolo-ng-common-binaries` |
| `ligolo-proxy` | Advanced, yet simple, tunneling/pivoting tool that uses a TUN interface Ligolo-ng is a simple, lightweight and fast tool that allows pentesters to establish tun | `sudo apt install ligolo-ng` |
| `minicom` | Friendly menu driven serial communication program Minicom is a clone of the MS-DOS “Telix” communication program. | `sudo apt install minicom` |
| `miredo` | Teredo IPv6 tunneling through NATs The Teredo IPv6 tunneling protocol encapsulates IPv6 packets into UDP/IPv4 datagrams, to allow hosts behind NAT devices to ac | `sudo apt install miredo` |
| `ncat` | NMAP netcat reimplementation ncat is a reimplementation of Netcat by the NMAP project, providing most of the features present in the original implementations, a | `sudo apt install ncat` |
| `netcat` | TCP/IP swiss army knife A simple Unix utility which reads and writes data across network connections using TCP or UDP protocol. | `sudo apt install netcat-traditional` |
| `penelope` | Powerful shell handler Penelope is a powerful shell handler built as a modern netcat replacement for RCE exploitation, aiming to simplify, accelerate, and optim | `sudo apt install penelope` |
| `powercat` | Netcat features all in powershell v2 | `sudo apt install powercat` |
| `proxytunnel` | Help SSH and other protocols through HTTP(S) proxies Proxytunnel creates tunnels through HTTP(S) proxies for any TCP based protocol. | `sudo apt install proxytunnel` |
| `ptunnel` | Tunnel TCP connections over ICMP packets ptunnel is an application that allows you to reliably tunnel TCP connections to a remote host using ICMP echo request a | `sudo apt install ptunnel` |
| `pwnat` | NAT to NAT client-server communication pwnat, pronounced “poe-nat”, is a tool that allows any number of clients behind NATs to communicate with a server behind  | `sudo apt install pwnat` |
| `raven` | Python tool that extends the capabilities of the http.server Python module | `sudo apt install raven` |
| `sbd` | Secure backdoor for Linux and Windows sbd is a Netcat-clone, designed to be portable and offer strong encryption. | `sudo apt install sbd` |
| `socat` | Multipurpose relay for bidirectional data transfer Socat (for SOcket CAT) establishes two bidirectional byte streams and transfers data between them. | `sudo apt install socat` |
| `sshuttle` | Transparent proxy server for VPN over SSH Sshuttle makes it possible to access remote networks using SSH. | `sudo apt install sshuttle` |
| `sslh` | Applicative protocol multiplexer sslh lets one accept HTTPS, SSH, OpenVPN, tinc and XMPP connections on the same port. | `sudo apt install sslh` |
| `stunnel4` | Universal SSL tunnel for network daemons The stunnel program is designed to work as SSL encryption wrapper between remote client and local (inetd-startable) or  | `sudo apt install stunnel4` |
| `udptunnel` | Tunnel UDP packets over a TCP connection UDPTunnel is a small program which can tunnel UDP packets bi-directionally over a TCP connection. | `sudo apt install udptunnel` |

## 提权(privesc)— 7 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `linpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `lynis` | Security auditing tool for Unix based systems Lynis is an auditing tool for hardening GNU/Linux and Unix based systems. | `sudo apt install lynis` |
| `peass` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `pspy` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `pspy-binaries` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `unix-privesc-check` | Script to check for simple privilege escalation vectors Unix-privesc-checker is a script that runs on Unix systems (tested on Solaris 9, HPUX 11, Various Linuxe | `sudo apt install unix-privesc-check` |
| `winpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |

## 提权(privesc)— 7 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `linpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `lynis` | Security auditing tool for Unix based systems Lynis is an auditing tool for hardening GNU/Linux and Unix based systems. | `sudo apt install lynis` |
| `peass` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `pspy` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `pspy-binaries` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `unix-privesc-check` | Script to check for simple privilege escalation vectors Unix-privesc-checker is a script that runs on Unix systems (tested on Solaris 9, HPUX 11, Various Linuxe | `sudo apt install unix-privesc-check` |
| `winpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |

## 提权(privesc)— 7 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `linpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `lynis` | Security auditing tool for Unix based systems Lynis is an auditing tool for hardening GNU/Linux and Unix based systems. | `sudo apt install lynis` |
| `peass` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `pspy` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `pspy-binaries` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `unix-privesc-check` | Script to check for simple privilege escalation vectors Unix-privesc-checker is a script that runs on Unix systems (tested on Solaris 9, HPUX 11, Various Linuxe | `sudo apt install unix-privesc-check` |
| `winpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |

## 提权(privesc)— 7 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `linpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `lynis` | Security auditing tool for Unix based systems Lynis is an auditing tool for hardening GNU/Linux and Unix based systems. | `sudo apt install lynis` |
| `peass` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `pspy` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `pspy-binaries` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `unix-privesc-check` | Script to check for simple privilege escalation vectors Unix-privesc-checker is a script that runs on Unix systems (tested on Solaris 9, HPUX 11, Various Linuxe | `sudo apt install unix-privesc-check` |
| `winpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |

## 提权(privesc)— 7 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `linpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `lynis` | Security auditing tool for Unix based systems Lynis is an auditing tool for hardening GNU/Linux and Unix based systems. | `sudo apt install lynis` |
| `peass` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `pspy` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `pspy-binaries` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `unix-privesc-check` | Script to check for simple privilege escalation vectors Unix-privesc-checker is a script that runs on Unix systems (tested on Solaris 9, HPUX 11, Various Linuxe | `sudo apt install unix-privesc-check` |
| `winpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |

## 提权(privesc)— 7 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `linpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `lynis` | Security auditing tool for Unix based systems Lynis is an auditing tool for hardening GNU/Linux and Unix based systems. | `sudo apt install lynis` |
| `peass` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `pspy` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `pspy-binaries` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `unix-privesc-check` | Script to check for simple privilege escalation vectors Unix-privesc-checker is a script that runs on Unix systems (tested on Solaris 9, HPUX 11, Various Linuxe | `sudo apt install unix-privesc-check` |
| `winpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |

## 提权(privesc)— 7 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `linpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `lynis` | Security auditing tool for Unix based systems Lynis is an auditing tool for hardening GNU/Linux and Unix based systems. | `sudo apt install lynis` |
| `peass` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |
| `pspy` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `pspy-binaries` | Monitor Linux processes without root permissions pspy is a command line tool designed to snoop on processes without need for root permissions. | `sudo apt install pspy` |
| `unix-privesc-check` | Script to check for simple privilege escalation vectors Unix-privesc-checker is a script that runs on Unix systems (tested on Solaris 9, HPUX 11, Various Linuxe | `sudo apt install unix-privesc-check` |
| `winpeas` | Privilege Escalation Awesome Scripts SUITE Privilege escalation tools for Windows and Linux/Unix* and MacOS. | `sudo apt install peass` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 取证/隐写(forensics)— 80 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `autopsy` | Graphical interface to SleuthKit The Autopsy Forensic Browser is a graphical interface to the command line digital forensic analysis tools in The Sleuth Kit. | `sudo apt install autopsy` |
| `binwalk` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for searching a given binary image for embedded files and executable code. | `sudo apt install binwalk` |
| `binwalk3` | Tool library for analyzing binary blobs and executable code Binwalk is a tool for identifying, and optionally extracting, files and data that have been embedded | `sudo apt install binwalk3` |
| `blkcalc` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `blkstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `bulk_extractor` | Extracts information without parsing filesystem bulk_extractor is a C++ program that scans a disk image, a file, or a directory of files and extracts useful inf | `sudo apt install bulk-extractor` |
| `chkrootkit` | Rootkit detector The chkrootkit security scanner searches for signs that the system is infected with a ‘rootkit’. | `sudo apt install chkrootkit` |
| `dc3dd` | Patched version of GNU dd with forensic features dc3dd is a patched version of GNU dd with added features for computer forensics: Convert and copy a file | `sudo apt install dc3dd` |
| `dcfldd` | Enhanced version of dd for forensics and security dcfldd was initially developed at Department of Defense Computer Forensics Lab (DCFL). | `sudo apt install dcfldd` |
| `dd_rescue` | Data recovery and protection tool When your disk has crashed and you try to copy it over to another one, standard Unix tools like cp, cat, and dd will abort on  | `sudo apt install ddrescue` |
| `ext3grep` | Tool to help recover deleted files on ext3 filesystems ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3 filesystem, o | `sudo apt install ext3grep` |
| `ext4magic` | Recover deleted files from ext3 or ext4 partitions ext4magic is a file carver (or file carving). | `sudo apt install ext4magic` |
| `extundelete` | Utility to recover deleted files from ext3/ext4 partition extundelete uses the information stored in the partition’s journal to attempt to recover a file that h | `sudo apt install extundelete` |
| `ffind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `fls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `foremost` | Forensic program to recover lost files Foremost is a forensic program to recover lost files based on their headers, footers, and internal data structures. | `sudo apt install foremost` |
| `fsstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `galleta` | Internet Explorer cookie forensic analysis tool Galleta is a forensics tool that examines the content of cookie files produced by Microsoft Internet Explorer (M | `sudo apt install galleta` |
| `grokevt-addlog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-builddb` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-findlogs` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-parselog` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `grokevt-ripdll` | Scripts for reading Microsoft Windows event log files GrokEVT is a collection of scripts built for reading Microsoft Windows NT/2000/XP/2003 event log files. | `sudo apt install grokevt` |
| `guymager` | Forensic imaging tool based on Qt The forensic imager contained in this package, guymager, was designed to support different image file formats, to be most user | `sudo apt install guymager` |
| `hashdeep` | Recursively compute hashsums or piecewise hashings hashdeep is a set of tools to compute MD5, SHA1, SHA256, tiger and whirlpool hashsums of arbitrary number of  | `sudo apt install hashdeep` |
| `hexwalk` | Hex analyzer, editor and viewer HexWalk is an Hex editor, viewer, analyzer based on opensource projects like qhexedit2, binwalk and QT It is cross platform and  | `sudo apt install hexwalk` |
| `hfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `icat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ifind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ils` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_cat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `img_stat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `istat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `jls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mactime` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `magicrescue` | Recover files by looking for magic bytes Magic Rescue scans a block device for file types it knows how to recover and calls an external program to extract them. | `sudo apt install magicrescue` |
| `missidentify` | Find win32 applications Miss Identify (missidentify) is a program to find MS Windows type win32 applications. | `sudo apt install missidentify` |
| `mmcat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmls` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `mmstat` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `myrescue` | Rescue data from damaged disks myrescue is a program to rescue the still-readable data from a damaged harddisk, CD-ROM, DVD, flash drives, etc. | `sudo apt install myrescue` |
| `outguess` | OutGuess is a universal tool for steganography that allows the insertion of hidden information into the redundant bits of data sources. | `sudo apt install outguess` |
| `pasco` | Internet Explorer cache forensic analysis tool Pasco is a forensic tool that examines the content of cache files (index.dat) produced by Microsoft Internet Expl | `sudo apt install pasco` |
| `pdf-parser` | Parses PDF files to identify fundamental elements This tool will parse a PDF document to identify the fundamental elements used in the analyzed file. | `sudo apt install pdf-parser` |
| `pdfid` | Scans PDF files for certain PDF keywords This tool is not a PDF parser, but it will scan a file to look for certain PDF keywords, allowing you to identify PDF d | `sudo apt install pdfid` |
| `photorec` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `readpe` | Library to read Windows PE files (development files) The libpe1t64 package provides a shared library which allows reading PE 32 and 64-bit files. | `sudo apt install libpe-dev` |
| `readpst` | Library for reading Microsoft Outlook PST files (development files) Library for accessing data from Microsoft Outlook PST files. | `sudo apt install libpst-dev` |
| `recoverdm` | Recover files on disks with damaged sectors recoverdm recover disks with bad sectors. You can recover files as well complete devices. | `sudo apt install recoverdm` |
| `recoverjpeg` | Recover JFIF (JPEG) pictures and MOV movies recoverjpeg tries to recover JFIF (JPEG) pictures and MOV movies from a peripheral. | `sudo apt install recoverjpeg` |
| `reglookup` | Utility to analysis for Windows NT-based registry (devel files) RegLookup is a system to direct analysis of Windows NT-based registry files providing command li | `sudo apt install libregfi-dev` |
| `regripper` | Perform forensic analysis of registry hives Regripper’s CLI tool can be used to surgically extract, translate, and display information (both data and metadata)  | `sudo apt install regripper` |
| `rifiuti` | MS Windows recycle bin analysis tool Rifiuti is a tool to examine the INFO2 files. | `sudo apt install rifiuti` |
| `rifiuti2` | Replacement for rifiuti, a MS Windows recycle bin analysis tool Rifiuti2 analyses recycle bin files from Windows. | `sudo apt install rifiuti2` |
| `rkhunter` | Rootkit, backdoor, sniffer and exploit scanner Rootkit Hunter scans systems for known and unknown rootkits, backdoors, sniffers and exploits. It checks for: | `sudo apt install rkhunter` |
| `safecopy` | Data recovery tool for problematic or damaged media Safecopy tries to get as much data from SOURCE as possible, even resorting to device specific low level oper | `sudo apt install safecopy` |
| `scalpel` | Fast filesystem-independent file recovery scalpel is a fast file carver that reads a database of header and footer definitions and extracts matching files from  | `sudo apt install scalpel` |
| `scrounge-ntfs` | Data recovery program for NTFS filesystems Scrounge NTFS is a data recovery program for NTFS filesystems. | `sudo apt install scrounge-ntfs` |
| `sigfind` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `sorter` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `srch_strings` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `ssdeep` | Recursive piecewise hashing tool (development headers) ssdeep is a tool for recursive computing and matching of Context Triggered Piecewise Hashing (aka Fuzzy H | `sudo apt install libfuzzy-dev` |
| `steghide` | Steganography hiding tool Steghide is steganography program which hides bits of a data file in some of the least significant bits of another file in such a way  | `sudo apt install steghide` |
| `stegosuite` | Steganography tool to hide information in image files Stegosuite is a graphical steganography tool to easily hide information in image files. | `sudo apt install stegosuite` |
| `stegsnow` | Steganography using ASCII files This utility can conceal messages in ASCII text by appending whitespaces to the end of lines. | `sudo apt install stegsnow` |
| `tailscale` | Secure connectivity platform | `sudo apt install tailscale` |
| `testdisk` | Partition scanner and disk recovery tool, and PhotoRec file recovery tool TestDisk checks the partition and boot sectors of your disks. | `sudo apt install testdisk` |
| `tsk_comparedir` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_gettimes` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_loaddb` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `tsk_recover` | Java Bindings for SleuthKit The Sleuth Kit, also known as TSK, is a collection of UNIX-based command line file and volume system forensic analysis tools. | `sudo apt install libsleuthkit-java` |
| `undbx` | Tool to extract, recover and undelete e-mail messages from .dbx files UnDBX is a tool to extract, recover and undelete e-mail messages from MS Outlook Express . | `sudo apt install undbx` |
| `unhide` | Forensic tool to find hidden processes and ports Unhide is a forensic tool to find processes and TCP/UDP ports hidden by rootkits, Linux kernel modules or by ot | `sudo apt install unhide` |
| `vinetto` | Forensics tool to examine Thumbs.db files vinetto is a console program to extract thumbnail pictures and their metadata from Thumbs.db files, that are generated | `sudo apt install vinetto` |
| `xplico-webui-start` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `xplico-webui-stop` | Network Forensic Analysis Tool (NFAT) The goal of Xplico is extract from an internet traffic capture the applications data contained. | `sudo apt install xplico` |
| `yara` | YARA development libraries and headers YARA is a tool aimed at helping malware researchers to identify and classify malware samples. | `sudo apt install libyara-dev` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 逆向/Fuzz(reversing)— 27 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `apktool` | Tool for reverse engineering Android apk files A tool for reverse engineering 3rd party, closed, binary Android apps. | `sudo apt install apktool` |
| `bed` | A network protocol fuzzer BED is a program which is designed to check daemons for potential buffer overflows, format strings et. al. | `sudo apt install bed` |
| `bytecode-viewer` | Java 8+ Jar & Android APK Reverse Engineering Suite | `sudo apt install bytecode-viewer` |
| `code-oss` | Open Source package of vscode | `sudo apt install code-oss` |
| `cstool` | Lightweight multi-architecture disassembly framework - command line tool Capstone is a lightweight multi-platform, multi-architecture disassembly framework. | `sudo apt install capstone-tool` |
| `cutter` | Development files to build cutter plugins These files can be used to build cutter plugins and integrate such plugin into the Cutter GUI. | `sudo apt install librizin-cutter-dev` |
| `d2j-dex2jar` | Tools to work with android .dex and java .class files dex2jar contains 4 compments: dex-reader is designed to read the Dalvik Executable (.dex/.odex) format. | `sudo apt install dex2jar` |
| `edb` | Cross platform x86/x86-64 debugger edb is a graphical cross platform x86/x86-64 debugger. | `sudo apt install edb-debugger` |
| `gdb` | GNU Debugger GDB is a source-level debugger, capable of breaking programs at any specific line, displaying variable values, and determining where errors occurre | `sudo apt install gdb` |
| `gef` | Modern experience for GDB with advanced debugging capabilities GEF is a set of commands for x86/64, ARM, MIPS, PowerPC and SPARC to assist exploit developers an | `sudo apt install gef` |
| `generic_chunked` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_listen_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_tcp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `generic_send_udp` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `ghidra` | Software Reverse Engineering Framework | `sudo apt install ghidra` |
| `jadx-gui` | Dex to Java decompiler | `sudo apt install jadx` |
| `javasnoop` | Intercept Java applications locally Normally, without access to the original source code, testing the security of a Java client is unpredictable at best and unr | `sudo apt install javasnoop` |
| `jd-gui` | GUI Java .class decompiler JD-GUI is a standalone graphical utility that displays Java source codes of “.class” files. | `sudo apt install jd-gui` |
| `msf-nasm_shell` | Framework for exploit development and vulnerability research The Metasploit Framework is an open source platform that supports vulnerability research, exploit d | `sudo apt install metasploit-framework` |
| `olefile` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `olevba` | Analyze MS OLE2 files and MS Office documents Tools to analyze Microsoft OLE2 files (also called Structured Storage, Compound File Binary Format or Compound Doc | `sudo apt install oletools` |
| `ollydbg` | 32-bit assembler level analysing debugger OllyDbg is a 32-bit assembler level analysing debugger for Microsoft Windows. | `sudo apt install ollydbg` |
| `pyinstaller` | Utility to bundle a Python application into a single package PyInstaller reads a Python script written by you. | `sudo apt install pyinstaller` |
| `radare2` | Libraries from the radare2 suite The project aims to create a complete, portable, multi-architecture, unix-like toolchain for reverse engineering. | `sudo apt install libradare2-6.0.0t64` |
| `rizin` | Arch independent files from the Rizin suite Rizin is a fork of the radare2 reverse engineering framework with a focus on usability, working features and code cl | `sudo apt install librizin-common` |
| `sfuzz` | Black Box testing utilities In the same vein as the Generic Protocol Framework, sfuzz is a really simple to use black box testing suite called Simple Fuzzer (wh | `sudo apt install sfuzz` |
| `sickle-pdk` | Payload development kit Sickle is a payload development kit originally created to aid in crafting shellcode, however it can be used in crafting payloads for oth | `sudo apt install sickle-pdk` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |

## 报告/靶场(reports)— 22 个

| 工具 | 用途 | 安装 |
|---|---|---|
| `arsenal-ng` | Go-based command library equipped with 200+ cybersecurity cheat-sheets Inspired by arsenal, rewritten from scratch with a focus on simplicity, speed and develop | `sudo apt install arsenal-ng` |
| `cherrytree` | Hierarchical note taking application CherryTree is a hierarchical note taking application, featuring rich text, syntax highlighting, images handling, hyperlinks | `sudo apt install cherrytree` |
| `cutycapt` | Utility to capture WebKit’s rendering of a web page CutyCapt is a small cross-platform command-line utility to capture WebKit’s rendering of a web page into a v | `sudo apt install cutycapt` |
| `defectdojo-start` | Security orchestration and vulnerability management platform | `sudo apt install defectdojo` |
| `dradis-start` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dradis-stop` | Collaboration tools for penetration testing Dradis is a tool to help you simplify reporting and collaboration. | `sudo apt install dradis` |
| `dvwa-start` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `dvwa-stop` | Damn Vulnerable Web Application | `sudo apt install dvwa` |
| `eyewitness` | Rapid web application triage tool EyeWitness is designed to take screenshots of websites, provide some server header info, and identify default credentials if p | `sudo apt install eyewitness` |
| `gemini-cli` | Open-source AI agent | `sudo apt install gemini-cli` |
| `juice-shop-start` | Insecure web application | `sudo apt install juice-shop` |
| `juice-shop-stop` | Insecure web application | `sudo apt install juice-shop` |
| `kali-tweaks` | Tool to adjust advanced configuration settings for Kali Linux This package provides tweaks for Kali Linux. This include things like: | `sudo apt install kali-tweaks` |
| `obsidian` | Private and flexible writing app that adapts to the way you think Obsidian stores notes on your device, so you can access them quickly, even offline. | `sudo apt install obsidian` |
| `pipal` | Statistical analysis on password dumps All this tool does is to give you the stats and the information to help you analyse the passwords. | `sudo apt install pipal` |
| `portspoof-start` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `portspoof-stop` | Enhance OS security through a set of techniques | `sudo apt install portspoof` |
| `recordmydesktop` | Captures audio-video data of a Linux desktop session The application produces an ogg-encapsulated theora-vorbis file. | `sudo apt install recordmydesktop` |
| `redeye-start` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `redeye-stop` | Tool to help you manage your data during a pentest operation | `sudo apt install redeye` |
| `shell-gpt` | Command-line productivity tool powered by AI large language models A command-line productivity tool powered by AI large language models (LLM). | `sudo apt install shell-gpt` |
| `witnessme` | Web Inventory tool | `sudo apt install witnessme` |
